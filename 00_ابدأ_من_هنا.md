# قالب أثر التعليمي — النسخة السهلة للنشر

هذه النسخة صُممت خصيصًا لتقليل مشاكل النشر قدر الإمكان.

## المطلوب منك فقط

1. فك الضغط.
2. أنشئ مستودع GitHub جديدًا وارفع **محتويات هذا المجلد نفسها** إلى جذر المستودع.
   - يجب أن ترى `package.json` و`src` و`public` مباشرة في الصفحة الرئيسية للمستودع.
   - لا ترفع المجلد كطبقة إضافية داخل المستودع.
3. افتح Google AI Studio → Build → Import from GitHub → اختر المستودع.
4. راجع الـPreview وعدّل البيانات من AI Studio.
5. عند كل تعديل: Push إلى GitHub.
6. للنشر:
   - إذا سمح Google AI Studio بالنشر لحسابك، انشر مباشرة.
   - إذا طلب Billing، لا تغيّر القالب؛ استخدم Vercel أو Netlify وربط GitHub مرة واحدة فقط. بعد ذلك كل Push جديد يعيد النشر تلقائيًا.

## لماذا هذه النسخة أسهل؟

- لا توجد اختبارات تمنع البناء.
- لا توجد أوامر `check` أو `lint` أو `security scan` داخل أمر النشر.
- لا توجد تطبيقات فرعية داخل `apps/`.
- أمر البناء الوحيد: `vite build`.
- مهيأة مسبقًا لـVercel وNetlify.
- تستخدم `HashRouter` حتى لا تحتاج إعدادات Rewrite معقدة.
- يوجد `npm start` بسيط إذا احتاجت منصة استضافة إلى تشغيل Node بعد البناء.

## أماكن التخصيص الأساسية

- هوية المعلم: `src/data/teacher.json`
- هيكل المنهج: `src/data/curriculum.json`
- محتوى الدروس: `src/data/lessons/`

## ممنوع

- لا تضف مجلد تطبيق آخر كامل داخل المشروع.
- لا تغيّر أمر `build` إلى تشغيل اختبارات قبل البناء.
- لا تضع أسرار API داخل ملفات الواجهة أو GitHub.
